#!/bin/sh
export SHIP_HOME="${SHIP_HOME:-$DATADIR/gdiffuser}"
mkdir -p "$SHIP_HOME"

# dir=$DATADIR/gdiffuser
# mkdir -p "$dir"
# cd "$dir"

# for f in gdiffuser.o2r decomp-recipes gamecontrollerdb.txt ; do
# 	f="$APPDIR"/bin/"$f" 
# 	if [ -e "$f" ]; then
# 		cp -af "$f" ./
# 	fi
# done
